<%= render 'shared/navbar' %>

<div class="content">
  <h1><%= @post.title %></h1>
  <img src='<%= @post.image_url%>'>
  <p class="blog-text"> <%= @post.content %> </p>
</div>

<br>

<%= render 'shared/footer' %>